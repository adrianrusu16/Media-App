[package]
name = "media_app_ffi"
version = "0.1.0"
edition.workspace = true
license.workspace = true
publish.workspace = true

[lib]
crate-type = ["cdylib", "staticlib", "rlib"]
name = "media_app_ffi"
path = "src/lib.rs"

[dependencies]
media_app_core = { path = "../app_core" }
